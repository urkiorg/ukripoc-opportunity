version: 0.1
backend:
    phases:
        build:
            commands:
                - "# Execute Amplify CLI with the helper script"
                - amplifyPush --simple
frontend:
    phases:
        preBuild:
            commands:
                - nvm use 10
                - yarn install
        build:
            commands:
                - yarn build:ci
    artifacts:
        baseDirectory: dist
        files:
            - "**/*"
    cache:
        paths:
            - node_modules/**/*
