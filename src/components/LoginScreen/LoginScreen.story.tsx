import React from "react";
import { storiesOf } from "@storybook/react";
import { LoginScreen } from "./index";

storiesOf("Components|LoginScreen", module).add("Default", () => (
    <LoginScreen />
));
